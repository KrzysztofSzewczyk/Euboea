
matrix:
  fast_finish: true
  sudo: true
  include:
    - language: c
      script:
        - sudo apt-get install gcc-multilib
        - make
      after_success:
        - bash <(curl -s https://codecov.io/bash)
    - language: c
      script:
        - sudo apt-get install gcc-multilib
        - make test
